services:
  - type: web
    name: msirms-frontend
    runtime: node
    runtimeVersion: 18
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://your-backend-url.render.com/api
